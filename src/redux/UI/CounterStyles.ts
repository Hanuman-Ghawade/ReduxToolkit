import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  reset: {
    padding: 50,
  },
  increment: {
    padding: 50,
  },
  decrement: {
    padding: 50,
  },
});

export default styles;
