import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 20,
    textAlign: 'center',
  },
  text: {
    textAlign: 'center',
    fontSize: 30,
  },
});

export default styles;
