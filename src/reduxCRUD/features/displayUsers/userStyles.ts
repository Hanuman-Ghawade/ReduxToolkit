import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    borderColor: 'yellow',
    borderRadius: 15,
    padding: 30,
    elevation: 15,
  },
  button: {
    padding: 25,
  },
});

export default styles;
